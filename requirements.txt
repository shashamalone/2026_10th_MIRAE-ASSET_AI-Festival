# 2026 미래에셋 AI Festival — 금융상품 Agent
# python3 -m pip install -r requirements.txt

# 데이터 파이프라인
pandas
numpy
openpyxl          # 주최측 xlsx 읽기
lxml              # DART corpCode.xml 파싱
requests

# 온톨로지
rdflib
pyoxigraph

# KnowledgeBase
psycopg[binary]   # PostgreSQL 드라이버 (스키마 벡터 인덱스)
pgvector          # vector 타입 + cosine 연산자(<=>). PostgreSQL 확장도 설치 필요

# Agent
langgraph
langchain
langchain-naver   # ChatClovaX / ClovaXEmbeddings (HyperCLOVA X)

# 서비스
fastapi
uvicorn

# 시각화 (선택 — EDA/viz)
pyvis
