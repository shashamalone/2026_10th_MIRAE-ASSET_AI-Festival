> 금융 데이터 간의 복합적인 관계와 의미를 깊이 있게 이해하고 추론하는 '**지능형 금융 비즈니스 엔진**'으로의 진화

## **온톨로지(Ontology) 및 지식 그래프** 

![image.png](image/image.png)



## **다중 소스 통합 검색(Multi-Source Retrieval)**

![image.png](image/image%201.png)

![image.png](image/image%202.png)



### LLM이 질문을 어떻게 분해하고 어떻게 출력할까?

- 질문: **"국내 배당형 ETF 중에서 분기 배당이고 운용보수가 낮은 상품을 추천해줘"**
  - 어떤 DB에서 검색할지 분해하는 전략:
    - 배당형/분기 배당: 복잡한 속성과 관계이므로 **그래프 DB** 할당.
    - 운용 보수 값: 명확한 수치이므로 **RDB (SQL)** 할당.
    - 추천 사유 설명: 비정형 텍스트이므로 **벡터 DB** 할당
- DB 구조 분리
  - **Vector DB:** 자연어 문장의 유사도 기반 의미 검색.
  - **RDB:** 정형 데이터의 수치 비교 및 SQL 기반의 정확한 필터링
  - **Graph DB:** 다단계(**Multi-hop**) 관계 탐색에 최적화. (예: "에코프로의 상장 자회사를 편입한 ETF의 운용사 조회"와 같은 복합 질의 해결)

  
- **단일 검색기보다 스키마 + 비즈니스 의미 전달이 필수**
  - "코스피 관련 ETF"를 일반 SQL 단일 쿼리로만 조회하면 이름에 코스피가 들어간 것만 찾게 되어 실제 정답 확률이 45%에 불과함.
  - RDB에 없는 **'총보수 = 운용보수 + 기타보수'** 같은 관계 등 비즈니스 의미를 지식그래프로 함께 전달해야 누락 없는 정확도 높은 답변 생성이 가능해짐.

## 하이브리드 라우팅 4단계

![image.png](image/image%203.png)

1. **질문 분석(Intent Analysis):** 사용자 질문의 의도를 정밀하게 파악.
2. **온톨로지 그라운딩:** 질문 내 키워드를 정의된 온톨로지 개념/엔티티와 매핑.
3. **쿼리 라우팅(Strategic Planning):** 어떤 DB를 조합할지 결정하는 **전략적 기획 단계**.
4. **다중 DB 조회 및 통합:** 각 DB에서 추출된 파편화된 정보를 무결성 검토 후 통합.
5. **LLM 답변 생성:** 통합된 시맨틱 근거를 바탕으로 환각 없는 최종 답변 생성

### 단계별 상세

![image.png](image/image%204.png)



**Semantic AI**

- **Semantic AI - 온톨로지 기반 검색**
  -  그래프 탐색과 DB 필터링을 결합하여, LLM의 답변이 사용자에게 나가기 전 비즈니스 규칙으로 한 번 더 이상한 답변을 걸러냄
- **예시 질문**: "에코프로의 상장 자회사를 편입한 ETF의 운용사는?"
  - 에코프로 -&gt; 자회사 -&gt; 상장 여부 -&gt; 편입 ETF -&gt; 운용사 등 여러 단계를 순회(Multi-hop)해야 하는 복잡한 추론 질문의 경우 그래프 DB가 파워풀함



&nbsp;

&nbsp;

![image.png](image/image%205.png)

![image.png](image/image%206.png)



★★★ **Federated Query Architecture (페더레이티드 쿼리 아키텍처)** ★★★

- 빠르고 강력한 필터링 도구인 SQL(RDB)과 벡터 DB, 그래프 DB를 융합하여 여러 소스의 결과를 결합하는 아키텍처가 필요
- **동작 방식**
  1. **Intent 분석 (Query Understanding &amp; Ontology Grounding)**
    - 사용자의 자연어 질문을 분석 후 온톨로지 상의 정확한 엔티티와 관계로 치환함.
  2. **라우터 (Query Router)**
    - 어떤 쿼리는 그래프 DB로, 어떤 필터는 RDB로 보낼지 가장 효율적인 검색 플랜을 짜고 분배(Routing)함

![image.png](image/image%207.png)



### **하이브리드 데이터베이스**

![image.png](image/image%208.png)

![image.png](image/image%209.png)

![image.png](image/image%2010.png)

![image.png](image/image%2011.png)

### **기대효과**

![image.png](image/image%2012.png)

## **Ontology는 AI의 Semantic Layer (4단계 핵심 기능)** 

- **엔티티 리졸루션(Entity Resolution)**
  - 자연어 질문을 의미 단위로 정교하게 분해함.
  - 서로 다른 문서에서 언급되는 '이 상품', '해당 ETF' 등이 동일 객체임을 식별하는 매핑 기술 적용
- **Entity Resolution (엔티티 식별) ★★**
  - 문서를 청크 단위로 쪼갤 때, 뒤 페이지 청크에서 상품명 없이 '이 ETF는'이라고만 명시되어 있어 LLM이 맥락을 놓치는 문제를 해결하는 핵심 기술임
  - 첫 페이지에서는 설명을 잘하지만, 청크단위로 데이터를 넣게 되면, 뒤 청크에서 앞의 내용과연결할 수없다는 문제가 생김
- **비즈니스 규칙 기반 답변 검증(Answer Validation)**
  - YTD(연초 대비, 올해 수익률) 검색 시 1월 1일 기준 데이터가 맞는지 확인하는 등, LLM이 비즈니스 규칙에 맞는 정답을 뽑아냈는지 검증함.
- **시맨틱 그라운딩(Semantic Grounding)**
  - 모델이 학습된 파라미터에 의존하지 않고, 제공된 데이터의 의미(Ground Truth) 내에서만 답변하도록 통제
  - LLM이 멋대로 상상하지 않고 비즈니스 의미와 실제 데이터가 있는 것에서만 답변 근거를 가져오도록 통제함.

---

