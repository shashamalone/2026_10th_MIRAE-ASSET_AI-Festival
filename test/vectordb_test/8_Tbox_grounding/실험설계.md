

• 네, 대부분은 5_semantic_schema_nl2sql에서 검증했습니다. 거기서 A는 physical schema만, B는 TBox를 추가하고, C는

  TBox와 business rule·binding hint를 추가해 NL2SQL과 multi-source routing을 비교했습니다. 다만 C의 binding은 실행

  계약이 아니라 Planner에게 주는 힌트였기 때문에 SQL Executability 22.6%, Execution Accuracy 0%, Engine Selection

  Accuracy 24.8%에 그쳤습니다. 따라서 실험 5는 “RDB schema를 metadata context에 넣는 것이 도움이 되는가”는 확인했지

  만, 지금 제안하신 TBox grounding → deterministic Physical Schema Binding → constrained Planner 구조를 완성해서 검

  증한 실험은 아닙니다.

› 그럼 이 실험을 실험 8에 하는건 의미가 있을까 아니면 실험 7의 결과를 기반으로 진행하는게 좋을까

• 실험 8로 진행하는 것이 의미 있습니다. 다만 실험 7을 다시 반복하는 방식이 아니라, 실험 5의 실패 원인을 실험 7에서

  만든 deterministic guard와 연결해 검증하는 실험으로 설계해야 합니다.

  실험 5는 TBox + binding hint까지만 시험했고, binding이 실행 계약이 아니어서 실패했습니다. 실험 7은

  route·schema·cutoff·Graph 실행을 결정적으로 제한하는 기반을 만들었으므로, 실험 8에서는 다음을 비교하면 됩니다.

  A: TBox grounding만

  B: TBox + RDB/Graph/Vector binding hint

  C: TBox + deterministic physical binding

  D: C + table/column/join/engine whitelist + validator

  핵심 비교 지표는 다음입니다.

  - TBox URI → physical location binding coverage

  - RDB table/column exact accuracy

  - JOIN path/key accuracy

  - engine selection accuracy

  - SQL executability

  - PostgreSQL execution exact

  - schema hallucination rate

  - unsupported route의 안전 중단률

  - 전체 latency

  따라서 실험 5는 baseline, 실험 7은 실행 안전장치, 실험 8은 TBox→Physical Binding의 효과를 검증하는 본 실험으로 연

  결하는 것이 가장 적절합니다. Graph supportedBy 문서가 아직 부족하므로 실험 8의 1차 범위는 RDB binding과 route 계

  약부터 검증하고, Graph·Vector physical binding은 후속 확장으로 두는 것이 안전합니다.