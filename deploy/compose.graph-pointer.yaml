services:
  graph:
    volumes:
      - oxigraph-active:/data
      - ./ontology:/tbox:ro
      - ./artifacts/graph_v2:/abox:ro

volumes:
  oxigraph-active:
    name: ${OXIGRAPH_ACTIVE_VOLUME:?OXIGRAPH_ACTIVE_VOLUME must be an exact validated volume name}
    external: true
